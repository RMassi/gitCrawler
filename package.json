{
  "name": "git-crawler",
  "version": "0.0.1",
  "description": "Craws upon github api and retrieves user statistic over a specific organization",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "RMassi",
  "license": "ISC",
  "dependencies": {
    "bower": "^1.5.2"
  }
}
